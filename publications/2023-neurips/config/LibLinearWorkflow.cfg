[PY_EXPERIMENTER]
provider = sqlite
database = lcdb
table = liblinear
cpu.max = 1

keyfields = openmlid:int, seed_outer:int, seed_inner:int, train_sizes:text, hyperparameters:text, monotonic:boolean
resultfields = result:LONGTEXT

[CUSTOM]
workflow_class = LibLinearWorkflow